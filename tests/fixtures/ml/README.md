# ML fixtures

`training_dataset_manifest_v1.json` is a frozen canonical serialization fixture for MLF-002. It is generated from a fixed `TrainingDatasetContract` and includes the expected SHA-256 content fingerprint.

The fixture is test evidence only. It is not a pipeline data source and must not become training truth.
