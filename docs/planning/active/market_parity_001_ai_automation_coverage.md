# MARKET-PARITY-001 — AI automation / architecture coverage

Status: **COMPLETE — bounded source-coverage excursion closed; active F5 freeze campaign resumed.**

## Evidence

A current first-party employer vacancy and an independent Bundesagentur market
listing established a concrete coverage regression case for the existing
discovery raster. The correction remains generic: AI architecture, AI automation,
agentic AI and AI governance vocabulary were added to the normal market-sensor
profiles instead of creating an employer-specific keyword exception.

Hornetsecurity enters Employer-Origin only through the canonical
`generic_origin:<company_key>` product family. The official career page is a
candidate origin; strict generic `proof=PASS` remains the sole activation
authority.

## Acquisition capability

The generic targeted-search layer supports a bounded finite first-party inventory
when the employer exposes concrete job-detail links but no deterministic
server-side keyword search. It proves each bounded detail with the existing
genuine-job proof and applies the target query locally to the proven title. The
same finite cohort is used for the impossible control query.

This is a reusable capability, not a Hornetsecurity parser.

## Authority correction

PR #948 removed the remaining market-sensor authority leaks. BA/StepStone sensor
observations may cross the sensor boundary only as company identity plus bounded
derived vocabulary. Sensor job URLs, external job IDs and raw sensor jobs are not
Product authority and do not create normal Bronze/Silver/Gold jobs.

The Hornet acceptance path is therefore deliberately split:

`BA live -> company evidence`

then independently:

`Hornet Origin candidate -> active generic Origin -> Origin Raw -> Origin Silver -> Origin Gold -> Product/Control Center`

`ba_raw`, `ba_silver` and `ba_gold` are not acceptance stages.

## Terminal acceptance

Canonical public source:

`main@523a04ce5dac19229235a1d564f25f37278f943b`

Productive main workflow run `35578519791` completed **SUCCESS** and proved:

- `hornetsecurity` is generic `proof=PASS`;
- BA live returned Hornetsecurity and persisted company-only sensor evidence;
- Hornetsecurity independently entered the canonical Employer-Origin path;
- the exact AI Automation Architect Germany vacancy reached Origin Raw, Silver
  and Gold;
- the exact canonical Product row reached the Control Center;
- terminal marker: `HORNET_TARGET_CONTROL_CENTER_PROJECTION=PASS`.

No application action was part of this work item. MARKET-PARITY-001 grants no
continuation authority beyond this bounded excursion; sequencing returns to the
pre-existing `F5 -> F6` freeze path.
