# MARKET-PARITY-001 — AI automation / architecture coverage

Status: bounded source-coverage excursion before returning to the active freeze campaign.

## Evidence

A current first-party employer vacancy and an independent Bundesagentur market
listing establish a concrete coverage regression case for the existing discovery
raster. The correction is intentionally generic: AI architecture, AI automation,
agentic AI and AI governance vocabulary are added to the normal market-sensor
profiles instead of creating an employer-specific keyword exception.

Hornetsecurity enters Employer-Origin through the canonical
`generic_origin:<company_key>` product family. The official career page is only a
candidate origin; strict generic `proof=PASS` remains the sole activation
authority.

## Acquisition capability

The generic targeted-search layer now supports a bounded finite first-party
inventory when the employer exposes concrete job-detail links but no deterministic
server-side keyword search. It proves each bounded detail with the existing
genuine-job proof and applies the target query locally to the proven title. The
same finite cohort is used for the impossible control query.

This is a reusable capability, not a Hornetsecurity parser.

## Acceptance

The main activation workflow fails closed unless:

1. `hornetsecurity` is in the canonical generic proof=PASS cohort;
2. the active `generic_origin:hornetsecurity` source persists the known public
   AI Automation Architect vacancy into Silver;
3. the configured Bundesagentur profile and its normal connector independently
   return the same employer/title family for the new search term.

No application action is part of this work item.
